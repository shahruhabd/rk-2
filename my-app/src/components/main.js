

function Main() {

    // const items = [];

    // fetch('https://fakestoreapi.com/products/1')
    //         .then(res=>res.json())
    //         .then(
    //             (result) => {
    //             this.setState({
    //                 isLoaded: true,
    //                 items: result.prodcuts
    //             });
    //         },
    //         (error) => {
    //             this.setState({
    //                 isLoaded: true,
    //                 error
    //             })
    //         }
    //     )

    return(
        <div>
            {/* {items.map(item => (
                key={item.name}
                {item.title}
                ))} */}
        </div>
    )
}

export default Main